
{
  "entities": {
    "AppConfig": {
        "title": "App Config",
        "description": "Stores global application configuration.",
        "type": "object",
        "properties": {
            "latest": {
                "type": "string",
                "description": "The latest available version of the app (e.g., '0.2')."
            },
            "appIcon": {
                "type": "string",
                "description": "Base64 or URL of the remote app icon."
            },
            "appIconType": {
                "type": "string",
                "enum": ["svg", "image"],
                "description": "The format of the remote icon."
            }
        },
        "required": ["latest"]
    },
    "User": {
      "title": "User",
      "description": "Represents a user in the system.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "User's display name or nickname. This is not unique."
        },
        "username": {
          "type": "string",
          "description": "User's unique handle, starting with '@'. This must be unique across all users."
        },
        "avatar": {
          "type": "string",
          "description": "URL to the user's avatar image."
        },
        "status": {
          "type": "string",
          "enum": ["online", "away", "offline"],
          "description": "User's current status."
        },
        "statusMessage": {
            "type": "string",
            "description": "A short status message from the user."
        },
        "hasSetNickname": {
            "type": "boolean",
            "description": "True if the user has set their nickname at least once."
        },
        "isBot": {
            "type": "boolean",
            "description": "True if the user is a bot account."
        },
        "isDeleted": {
            "type": "boolean",
            "description": "True if the user account has been deleted/anonymized."
        },
        "lastSeen": {
            "type": "object",
            "description": "Server timestamp of when the user was last active."
        },
        "infGoldBalance": {
            "type": "number",
            "description": "The user's balance of InfGold."
        },
        "subscriptionTier": {
            "type": "string",
            "enum": ["none", "super", "mega", "prem", "giga", "ultra"],
            "description": "The user's current subscription tier."
        },
        "lastDailyBonusClaimed": {
            "type": "object",
            "description": "Server timestamp of when the user last claimed their daily bonus."
        },
        "birthday": {
          "type": "object",
          "properties": {
            "day": { "type": "number" },
            "month": { "type": "number" },
            "year": { "type": "number" }
          },
          "required": ["day", "month"]
        }
      },
      "required": ["name", "username", "avatar", "status"]
    },
    "Username": {
        "title": "Username",
        "description": "Maps a unique username to a user ID to enforce uniqueness.",
        "type": "object",
        "properties": {
            "uid": {
                "type": "string",
                "description": "The user ID associated with this username."
            }
        },
        "required": ["uid"]
    },
    "Message": {
      "title": "Message",
      "description": "A single message within a chat.",
      "type": "object",
      "properties": {
        "senderId": {
          "type": "string",
          "description": "The ID of the user who sent the message."
        },
        "content": {
          "type": "string",
          "description": "The text content of the message."
        },
        "imageUrl": {
            "type": "string",
            "description": "URL to an image attachment, in Base64 data URI format."
        },
        "timestamp": {
          "type": "object",
          "description": "The server timestamp when the message was created."
        },
        "editedAt": {
            "type": "object",
            "description": "The server timestamp when the message was last edited."
        },
        "type": {
          "type": "string",
          "enum": ["user", "announcement"],
          "description": "The type of message."
        },
        "reactions": {
            "type": "object",
            "additionalProperties": {
                "type": "array",
                "items": { "type": "string" }
            }
        }
      },
      "required": ["senderId", "timestamp"]
    },
    "Chat": {
      "title": "Chat",
      "description": "Represents a chat conversation.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["dm", "group", "channel"]
        },
        "name": {
          "type": "string",
          "description": "Name of the group or channel."
        },
        "members": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of user IDs who are members of this chat."
        },
        "ownerId": {
          "type": "string",
          "description": "The user ID of the owner of the group or channel."
        },
        "icon": {
          "type": "string",
          "description": "Name of the lucide-react icon for the chat."
        },
        "avatar": {
            "type": "string",
            "description": "URL to the chat's avatar image."
        }
      },
      "required": ["type", "members"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/config/version": {
      "schema": {
        "$ref": "#/backend/entities/AppConfig"
      },
      "description": "Stores global application configuration like the latest version and remote icon."
    },
    "/users/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/User"
      },
      "description": "Stores individual user profiles."
    },
    "/usernames/{username}": {
        "schema": {
            "$ref": "#/backend/entities/Username"
        },
        "description": "Used to enforce unique usernames."
    },
    "/chats/{chatId}": {
      "schema": {
        "$ref": "#/backend/entities/Chat"
      },
      "description": "Stores metadata for chat rooms (DMs, groups, channels)."
    },
    "/chats/{chatId}/messages/{messageId}": {
      "schema": {
        "$ref": "#/backend/entities/Message"
      },
      "description": "Stores messages for a specific chat."
    }
  }
}
